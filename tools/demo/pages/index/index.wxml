<block wx:for="{{list}}" wx:key="index">
    <wechat-long-list-chunk 
        chunkId="{{'_chunk_id_'+index}}"
        showLogInfo="{{true}}"
        >
        <card-list list="{{item}}"></card-list>
    </wechat-long-list-chunk>
</block>

